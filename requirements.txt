numpy>=1.21.0
Pillow>=9.0.0
flask>=2.0.0
mss>=6.0.0
pywebview>=4.0.0
pytest>=7.0.0
pytest-timeout>=2.3.0
# Required for reading raw float EXR depth maps emitted by renderdoccmd
# exportframe (FileType::EXR). cv2 is the lightest install on Windows;
# imageio[freeimage] / OpenEXR also work if cv2 is unavailable. The
# RDOC depth pipeline (grabbers/renderdoc/image_loader.load_depth_image)
# tries them in that order.
opencv-python>=4.5.0
